Unlock your Case Narrative account

Hello <%= @resource.name.presence || @resource.email %>,

Your account was locked after several unsuccessful sign-in attempts. Unlock it using this secure link:

<%= unlock_url(@resource, unlock_token: @token) %>

If these sign-in attempts weren't yours, unlock the account and reset your password before signing in again.
